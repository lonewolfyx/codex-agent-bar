import AppKit
import SwiftUI

enum QuotaPalette {
    static let warningNSColor = NSColor(calibratedRed: 0.86, green: 0.49, blue: 0.04, alpha: 1)
    static let warningColor = Color(nsColor: warningNSColor)
}
